spring:
  application:
    name: terrible-task-launcher

  cloud:
    stream:
      bindings:
        input:
          destination: terrible-task-launcher
    deployer:
      local:
        maximum-concurrent-tasks: 5
